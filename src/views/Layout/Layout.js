import React from 'react';
import PropTypes from 'prop-types';

const Layout = (props) => {
    const { } = props;
    return (
        <div>
            <h1>Layout</h1>
            <ul></ul>
        </div>
    )
}

export default Layout;